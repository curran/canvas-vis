// Generated by CoffeeScript 1.6.1
(function() {
  var __slice = [].slice;

  define(['backbone', 'underscore'], function(Backbone, _) {
    var expose;
    expose = function() {
      var model, properties;
      model = arguments[0], properties = 2 <= arguments.length ? __slice.call(arguments, 1) : [];
      return _.each(properties, function(property) {
        var p;
        p = property;
        model.__defineGetter__(p, function() {
          return model.get(p);
        });
        return model.__defineSetter__(p, function(val) {
          return model.set(p(val));
        });
      });
    };
    return Backbone.Model.extend({
      initialize: function() {
        return expose(this, 'x', 'y', 'w', 'h');
      }
    });
  });

}).call(this);
